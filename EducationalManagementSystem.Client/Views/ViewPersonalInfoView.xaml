<UserControl x:Class="EducationalManagementSystem.Client.Views.ViewPersonalInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EducationalManagementSystem.Client.Views"
             xmlns:viewmodels="clr-namespace:EducationalManagementSystem.Client.ViewModels"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" IsVisibleChanged="VisiblilityChanged">
    <UserControl.DataContext>
        <viewmodels:ViewPersonalInfoViewModel/>
    </UserControl.DataContext>
    <Grid>
        <GroupBox Header="用户信息：" Margin="100,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="1" Grid.Column="0" Text="姓名：" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding User.Name}"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="用户ID：" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding User.UserID}"/>
                <TextBlock Grid.Row="5" Grid.Column="0" Text="性别：" HorizontalAlignment="Right"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding User.Gender, Converter={StaticResource GenderConverter}}"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
